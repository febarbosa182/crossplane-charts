apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-default-addons.fullname" . }}
  labels:
    {{- include "aws-default-addons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-default-addons.selectorLabels" . | nindent 4 }}
